@model IPagination<MVCContribGrid.Models.Employee>
@using MvcContrib.UI.Grid;
@using MvcContrib.Pagination;
@using MvcContrib.UI.Pager;
@using MvcContrib.Sorting;



<style>

    .grid {
        font-family: Arial,Helvetica,sans-serif;
        font-size: 14px;
        font-weight: normal;
        width: 650px;
        display: table;
        border-collapse: collapse;
        border: solid 1px #C5C5C5;
        background-color: white;
    }

        .grid td, th {
            border: 1px solid #C5C5C5;
            padding: 3px 7px 2px;
        }

        .grid thead, .header a {
            background-color: #0094ff;
            color: #ffffff;
            text-align: left;
            text-decoration: none;
        }

    .grid-footer {
    }

    .grid-row-style {
        padding: 3px 7px 2px;
    }

    .grid-alternating-row {
        background-color: azure;
        padding: 3px 7px 2px;
    }

    .col1Width {
        width: 55px;
    }

    .col2Width {
        width: 220px;
    }
</style>

<h2>MVCContrib Grid - Index</h2>
<div id="pager">
    @Html.Pager((IPagination)Model) 
</div>
<p>
    @Html.ActionLink("Create New", "Create")
    @Html.Grid(Model).AutoGenerateColumns().Sort((GridSortOptions)ViewData["sort"])
   
</p>
@*<p>
    @Html.Grid(Model).Columns(col =>
{
    col.For(p => p.Name);
    col.For(p => p.Designation);
    col.For(p => p.Gender);
    col.For(p => p.Salary);
    col.For(p => p.City);
    col.For(p => p.State);
    col.For(p => p.Zip);
    col.For(p => Html.ActionLink("Edit", "Edit", "MVCContrib", new { id = p.id }, new { }));
    col.For(p => Html.ActionLink("Details", "Details", "MVCContrib", new { id = p.id }, new { }));
    col.For(p => Html.ActionLink("Delete", "Delete", "MVCContrib", new { id = p.id }, new { }));
}).Attributes(id => "example")
</p>*@
